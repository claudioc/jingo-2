<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="generator" content="jingo wiki">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{urlFor resource="css" id="styles.css"}}">
    <link rel="stylesheet" href="{{urlFor resource="vendor" id="tufte-css/tufte.css"}}">
    {{{customStyles}}}
    <title>{{title}}</title>
  </head>
  <body>
    <nav class="sans">
      <a href="{{urlFor resource="home"}}">Home</a>
      <a href="{{urlFor resource="wiki"}}">Documents list</a>
      <a href="{{urlFor resource="doc" into=dirName}}">New doc here</a>
      <a href="{{urlFor resource="folder" into=dirName}}">New folder here</a>
    </nav>
    {{{body}}}
    {{{customScripts}}}
  </body>
</html>
